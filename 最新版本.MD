对，但有一个关键点要修正：

**不是把 JPG 直接传进 BRAM，也不是让协处理器自己访问 BRAM。**

正确的数据流是：

```text
PC上的 JPG/PNG
   ↓ Python/Pillow
转成 256×256 灰度 RAW（65536 Byte）
   ↓ UART
PicoRV32 固件接收
   ↓
写入 BRAM
   ↓
PicoRV32: LW 读取4个像素
   ↓
rs1/rs2
   ↓ PCPI
XPix 协处理器执行 4×8-bit SIMD 自定义指令
   ↓
rd 返回32-bit结果
   ↓
PicoRV32: SW 写回 BRAM
   ↓
UART 回传 RAW
   ↓
PC恢复成 PNG 显示
```

也就是说 **XPix 是“计算协处理器”，不直接碰内存**。例如核心循环最终类似：

```c
uint32_t a = frame_a[i];     // LW，一次4像素
uint32_t b = frame_b[i];     // LW，一次4像素

uint32_t d = px_absdiff8(a, b);   // PCPI custom instruction
uint32_t r = px_thresh8(d, th4);  // PCPI custom instruction

output[i] = r;               // SW
```

这比让 XPix 自己访问 BRAM 更适合这门课，因为这样真正展示的是 **“CPU 指令集扩展”**，而不是把东西做成一个 DMA/图像加速 IP。

你现在可以直接把下面这份 Prompt 丢到 Work/Codex 工作区。它是按你现有 **`picorv32-main` + ModelSim/Questa `vsim` + Vivado** 环境写的，目标是一次把**上板前的软件、RTL、仿真、综合验证框架**搭完整。

---

# 可直接使用的 Prompt

```text
你现在是我的 FPGA / RISC-V / RTL 工程开发代理。

工作区中已经有：

1. PicoRV32 原始源码仓库 picorv32-main
2. ModelSim / QuestaSim 环境，可使用 vsim / vlog
3. Xilinx Vivado 环境
4. 最终 FPGA 平台是 Xilinx ZCU102
5. 当前阶段目标是“完成真正上 ZCU102 之前的全部前端开发和验证”，暂时不要依赖实物板卡。

============================================================
一、总项目目标
============================================================

基于现有 PicoRV32 开源 CPU，构建一个：

    XPix-RV32

即：

    PicoRV32 RV32I CPU
        +
    PCPI 自定义指令协处理器
        +
    4×8-bit Packed-SIMD 图像处理指令
        +
    BRAM
        +
    UART
        +
    图像 benchmark / 性能测试

最终用于 ZCU102 FPGA 原型验证。

项目目的不是单纯“添加几条指令”，而是证明：

1. 自定义图像指令功能正确；
2. 一条自定义指令同时处理4个8-bit像素；
3. 图像 kernel 相比普通 RV32I 得到明显加速；
4. 对普通 RV32I 程序不引入执行周期退化；
5. XPix 带来的 LUT/FF/Fmax 开销可控；
6. RTL 仿真、CPU级程序仿真、Vivado 综合都能够通过；
7. 为后续 ZCU102 UART 实物上板准备完整工程。

本阶段必须实际修改代码、运行仿真、运行编译和 Vivado 验证。
不要只给设计建议。

每完成一个阶段都必须实际执行对应测试并记录结果。
发现问题立即修复，不允许只留下 TODO。

============================================================
二、首先审计现有 PicoRV32 仓库
============================================================

首先阅读整个工作区和 PicoRV32 仓库结构。

重点检查：

    picorv32.v
    picosoc/
    firmware/
    tests/
    scripts/
    testbench.v
    Makefile

确认并形成 docs/baseline_audit.md：

1. PicoRV32 微架构特点；
2. 默认参数配置；
3. 当前支持的 RV32I 指令；
4. MUL/DIV/Compressed ISA 是否默认打开；
5. PCPI 接口工作方式；
6. picorv32_pcpi_mul / div 如何实现握手；
7. memory native interface；
8. PicoSoC 中 simpleuart.v 的使用方式；
9. cycle / instret counter 的使用方式；
10. 当前测试基础设施。

不要凭印象总结，必须以仓库实际 RTL 为准。

原则：

尽量不要修改原始 picorv32.v。

如果确实必须修改，先说明理由。
优先通过它已经提供的 PCPI interface 实现 custom instruction。

============================================================
三、基础 CPU 配置
============================================================

本项目 baseline CPU 采用 RV32I。

建议基础配置：

    ENABLE_COUNTERS       = 1
    ENABLE_COUNTERS64     = 1
    ENABLE_REGS_16_31     = 1
    ENABLE_REGS_DUALPORT  = 1

    BARREL_SHIFTER        = 1

    COMPRESSED_ISA        = 0

    ENABLE_MUL            = 0
    ENABLE_FAST_MUL       = 0
    ENABLE_DIV            = 0

    ENABLE_IRQ            = 0

    CATCH_MISALIGN        = 1
    CATCH_ILLINSN         = 1

XPix版本：

    ENABLE_PCPI           = 1

Baseline版本：

    ENABLE_PCPI           = 0

如果 PicoRV32 实际参数名称与上述存在差异，以源码为准。

============================================================
四、设计 XPix 自定义 ISA
============================================================

使用 RISC-V CUSTOM-1 opcode：

    opcode = 7'b0101011
           = 0x2B

采用 R-type 编码：

    funct7 | rs2 | rs1 | funct3 | rd | opcode

第一版：

    funct7 = 7'b0000000

定义至少以下五条指令：

funct3 = 000
    PX.ABSDIFF8

funct3 = 001
    PX.THRESH8

funct3 = 010
    PX.ADDUS8

funct3 = 011
    PX.MAXU8

funct3 = 100
    PX.MINU8


所有指令把32-bit寄存器看成：

    rs[7:0]    pixel0
    rs[15:8]   pixel1
    rs[23:16]  pixel2
    rs[31:24]  pixel3

即：

    4 × uint8

----------------------------------
1. PX.ABSDIFF8
----------------------------------

每个lane：

    rd[i] = abs(rs1[i] - rs2[i])

必须是无符号 8-bit absolute difference。

----------------------------------
2. PX.THRESH8
----------------------------------

每个lane：

    rd[i] = rs1[i] >= rs2[i] ? 8'hFF : 8'h00

rs2通常由软件构造为：

    {T,T,T,T}

----------------------------------
3. PX.ADDUS8
----------------------------------

无符号饱和加：

    sum = rs1[i] + rs2[i]

如果：

    sum > 255

输出：

    255

否则：

    sum

绝对不能出现 uint8 wrap-around。

----------------------------------
4. PX.MAXU8
----------------------------------

    rd[i] = max(rs1[i],rs2[i])

----------------------------------
5. PX.MINU8
----------------------------------

    rd[i] = min(rs1[i],rs2[i])

============================================================
五、XPix PCPI 微架构
============================================================

新增：

    rtl/xpix_pcpi.v

不要让 XPix 直接访问 BRAM。

XPix 只通过：

    pcpi_valid
    pcpi_insn
    pcpi_rs1
    pcpi_rs2

获得操作数。

通过：

    pcpi_wr
    pcpi_rd
    pcpi_wait
    pcpi_ready

返回结果。

优先采用稳定、容易 timing closure 的 registered 实现。

建议：

STATE_IDLE
    ↓
识别 CUSTOM-1
锁存：
    instruction
    rs1
    rs2

STATE_EXEC
    ↓
执行4 lane SIMD运算

STATE_DONE
    ↓
    pcpi_wr    = 1
    pcpi_ready = 1
    pcpi_rd    = result

随后返回 IDLE。

要求：

1. 不属于 XPix 的 PCPI instruction 不得错误响应；
2. 对合法 XPix 指令完成 ready；
3. 不允许出现 handshake deadlock；
4. 连续执行 XPix 指令必须正常；
5. rd=x0 的 CPU行为应保持符合 PicoRV32；
6. reset 后状态正确；
7. combinational logic 不得产生 latch；
8. 所有时序逻辑使用 nonblocking assignment；
9. 尽量保持普通 PicoRV32 datapath 不变。

完成：

    docs/xpix_isa.md

包含：

    指令编码
    funct3定义
    每个lane语义
    输入输出示例
    PCPI时序说明

============================================================
六、建立独立 XPix RTL testbench
============================================================

新增：

    sim/tb_xpix_pcpi.v

先完全脱离 CPU 测试 XPix。

必须包含 directed tests。

ABSDIFF：

    0,0
    0,255
    255,0
    128,128
    200,100

ADDUS：

    0+0
    255+0
    255+1
    128+127
    128+128
    255+255

THRESH：

T=128：

    0
    127
    128
    129
    255

MAX/MIN：

包含：

    相等
    极值
    交叉大小关系

然后做不少于：

    100000组

随机 rs1 / rs2 / funct3 测试。

testbench 自己实现 reference model。

任何 mismatch：

    $fatal

最终打印：

    XPIX_UNIT_TEST_PASS
    random_cases=100000
    mismatch=0

必须实际使用 vlog + vsim 运行。

生成：

    sim/run_xpix.do

使我可以：

    vsim -c -do sim/run_xpix.do

一键运行。

============================================================
七、建立 CPU + XPix SoC
============================================================

建立清晰工程结构，例如：

    rtl/
        picorv32.v
        xpix_pcpi.v
        xpix_soc.v
        soc_bram.v
        simpleuart.v
        zcu102_top.v

    firmware/
        start.S
        sections.lds
        uart.c
        uart.h
        xpix.h
        image_bench.c

    sim/
        tb_xpix_pcpi.v
        tb_soc.v
        run_xpix.do
        run_soc.do

    host/
        make_test_image.py
        image_to_raw.py
        raw_to_image.py
        uart_host.py

    vivado/
        create_project.tcl
        run_synth.tcl

    docs/
    reports/

如果仓库已有更适合结构，不必机械照搬，但必须清晰。

============================================================
八、BRAM / memory map
============================================================

当前先设计一个仿真和综合都可用的 memory subsystem。

最终目标至少容纳：

    firmware
    Frame A
    Frame B
    Scalar output
    XPix output

图像默认：

    256 × 256
    uint8 grayscale

一张图：

    65536 bytes
    64 KiB

建议数据空间至少：

    512 KiB

但需要考虑 ZCU102 Vivado BRAM 推断。

不要使用：

    initial $readmemh

作为最终上板数据路径的唯一方案。

仿真可以使用 memory preload，
实际系统必须支持 CPU/UART 写入图像区域。

制定明确 memory map。

例如可以设计：

    firmware/code
    frame_a
    frame_b
    output_scalar
    output_xpix
    stack

具体地址由你根据 PicoRV32 reset PC / linker script 合理决定。

要求：

1. word access正确；
2. byte write支持正确；
3. PicoRV32 native memory handshake正确；
4. little-endian行为明确；
5. alignment正确。

将最终 memory map 写入：

    docs/memory_map.md

============================================================
九、UART
============================================================

优先复用仓库：

    picosoc/simpleuart.v

不要无意义重新设计 UART。

构建 memory mapped UART。

至少支持：

    RX byte
    TX byte
    baud divider/status

软件函数：

    uart_putc()
    uart_getc()
    uart_write()
    uart_read()

仿真 testbench 中建立 UART model，
至少验证 CPU 可以：

    输出字符串
    接收数据
    回传数据

如果完整 bit-level UART 仿真太慢，
可以：

1. 保留真正 simpleuart RTL；
2. 同时为快速 SoC benchmark 提供 simulation-only 加速数据加载方法；

但二者必须分开，不能为了仿真方便破坏最终硬件结构。

============================================================
十、Host 图像工具
============================================================

PC 端不要向 FPGA 传 JPG/PNG 压缩数据。

FPGA/PicoRV32 不负责 JPEG/PNG 解码。

新增 Python：

    host/image_to_raw.py

功能：

输入：

    jpg/png/bmp

使用 Pillow：

    convert("L")
    resize((256,256))

输出：

    uint8 raw

即：

    65536 bytes

新增：

    host/raw_to_image.py

将：

    65536 byte raw

恢复：

    PNG

再新增：

    host/make_test_image.py

自动生成至少两组 deterministic test images：

A. moving_square_a.png
B. moving_square_b.png

例如：

    256×256灰度背景
    frame A 白色方块在位置1
    frame B 白色方块移动到位置2

用于运动检测验证。

再生成：

    gradient.png

用于饱和加法测试。

要求 Python 也提供 software golden model：

    absdiff
    threshold
    saturating add
    max
    min

后续用于和 FPGA/RTL 结果比较。

============================================================
十一、软件调用 Custom Instruction
============================================================

不要修改 GCC/binutils。

在：

    firmware/xpix.h

使用 GNU inline assembly `.insn`。

例如：

    .insn r 0x2b, funct3, funct7, rd, rs1, rs2

注意确认当前 assembler 实际语法。

需要实现：

    px_absdiff8()
    px_thresh8()
    px_addus8()
    px_maxu8()
    px_minu8()

编译 firmware。

必须使用 objdump 确认最终 binary 中确实生成：

    opcode 0x2B

而不是编译器偷偷替换掉。

保存反汇编：

    reports/xpix_firmware.dump

============================================================
十二、三个软件 baseline
============================================================

性能测试不能只做：

    scalar C
    vs custom

必须提供三个版本：

----------------------------------
A. Scalar C
----------------------------------

每次处理一个 uint8 pixel。

完全使用普通 RV32I。

例如 ABS：

    uint8_t a
    uint8_t b
    if / subtract

----------------------------------
B. Packed RV32I
----------------------------------

每次使用：

    uint32_t

从 memory 加载4个像素：

    LW
    LW

但不能使用 XPix。

必须只依靠标准 RV32I：

    shift
    mask
    compare
    add
    etc.

这样排除：

“一次 LW 4个像素本身产生的收益”。

----------------------------------
C. Packed XPix
----------------------------------

使用：

    LW
    LW
    PX...
    SW

这样最终可以比较：

    XPix vs Scalar

以及更重要的：

    XPix vs Packed-RV32I

后者才代表更纯粹的 ISA acceleration。

============================================================
十三、Image kernels
============================================================

至少实现：

1.

    absdiff

2.

    threshold

3.

    saturating brightness / add

4.

    max fusion

5.

    min fusion

以及主应用：

    Motion Detection

流程：

    Frame A
        \
         PX.ABSDIFF8
        /
    Frame B
         ↓
    PX.THRESH8
         ↓
    Motion Mask

例如 threshold：

    30 或 40

软件应支持参数修改。

============================================================
十四、正确性验证
============================================================

CPU 仿真中，对每个算法：

先运行 scalar reference：

    output_scalar

再运行 XPix：

    output_xpix

最后：

    byte-by-byte compare

必须：

    mismatch_count == 0

尤其 Motion Detection：

    65536 pixels

必须完全 bit exact。

测试结果输出例如：

    ABS mismatch = 0
    THRESH mismatch = 0
    ADDUS mismatch = 0
    MAX mismatch = 0
    MIN mismatch = 0
    MOTION mismatch = 0

任何非零 mismatch：

    FAIL

============================================================
十五、Cycle / instruction 性能测试
============================================================

使用 PicoRV32 已有：

    cycle counter
    instret counter

不要使用仿真 wall-clock 时间代表 CPU 性能。

对：

    Scalar
    Packed RV32I
    XPix

记录：

    cycle count
    instruction retired

例如输出：

    === ABS ===

    scalar_cycles
    scalar_instret

    packed_cycles
    packed_instret

    xpix_cycles
    xpix_instret

计算：

    speedup_xpix_vs_scalar

    speedup_xpix_vs_packed

同样对：

    threshold
    addus
    max
    min
    motion pipeline

进行测试。

输出生成：

    reports/performance.csv

至少列：

    workload
    scalar_cycles
    packed_cycles
    xpix_cycles

    scalar_instret
    packed_instret
    xpix_instret

    speedup_vs_scalar
    speedup_vs_packed

不要伪造任何数字。

只能填写真正 simulation 得到的数据。

============================================================
十六、CPU regression
============================================================

必须验证加入 XPix 后没有破坏原 PicoRV32。

尽可能使用仓库现有：

    tests/
    firmware/
    testbench

至少运行一部分现有 RV32I instruction regression。

同时建立一个普通非 XPix 程序：

例如：

    integer arithmetic
    memory loop
    branch loop

比较：

Baseline PicoRV32
和
PicoRV32 + XPix

要求：

不使用自定义指令时：

    algorithm CPU cycles相同

允许综合后 Fmax/面积有变化，
但是 ISA行为和运行周期不应该因为 XPix 而被额外 stall。

输出：

    reports/regression.md

============================================================
十七、ModelSim / Questa 自动化
============================================================

所有关键仿真必须支持 CLI。

提供：

    sim/run_all.sh
或者：
    sim/run_all.do

至少依次运行：

1. XPix unit test
2. CPU custom instruction test
3. UART basic test
4. image correctness
5. performance benchmark
6. RV32I regression

最终输出：

    ALL_TESTS_PASS

如果工作区是 Windows，
可提供：

    .bat / .do / PowerShell

根据实际环境选择。

不要假定 Linux。

先检查当前环境。

============================================================
十八、Vivado 工程
============================================================

必须创建可重复执行的 Tcl 工程，不要只依赖 GUI。

目标器件依据 ZCU102：

    XCZU9EG

先确认具体 part。

一般 ZCU102 常见：

    xczu9eg-ffvb1156-2-e

但不要盲目硬编码。
检查工作区/board files/Vivado支持的 ZCU102 part。

提供：

    vivado/create_project.tcl

能够建立：

    baseline
和
    xpix

两个 build。

当前阶段可以使用一个 synthetic clock port，
不要求立即完成所有实物 pin constraints。

但是必须有：

    create_clock

例如初始目标：

    125 MHz
    period = 8.000 ns

============================================================
十九、Vivado 综合比较
============================================================

至少综合两个版本：

A.

    PicoRV32 baseline

B.

    PicoRV32 + XPix

如果方便，再增加：

C.

    PicoRV32 + PCPI dummy

便于拆分：

    PCPI interface cost
    XPix arithmetic cost

运行：

    synth_design
    opt_design

至少生成：

    report_utilization
    report_timing_summary

如果能够在当前环境合理执行：

    place_design
    route_design

也执行 implementation。

最终分别保存：

    reports/vivado/baseline_utilization.rpt
    reports/vivado/baseline_timing.rpt

    reports/vivado/xpix_utilization.rpt
    reports/vivado/xpix_timing.rpt

并生成：

    reports/ppa_summary.md

表格至少包含：

    LUT
    FF
    BRAM
    DSP

    WNS
    clock target

如果做了 implementation：

    post-route timing

不要伪造所谓 Fmax。

如果没有实际 sweep，
不要声称具体最大 Fmax。

125 MHz timing clean 可以明确报告。

============================================================
二十、Timing设计原则
============================================================

非常重要：

不能为了做自定义指令导致 PicoRV32 原 ALU critical path 明显恶化。

因此：

1. XPix 不插到原 RV32I ALU combinational path；
2. 使用独立 PCPI；
3. XPix 允许多周期；
4. 优先通过 pipeline/register 保证 timing；
5. 首要目标是：
       baseline @125MHz PASS
       xpix @125MHz PASS

如果失败：

必须分析：

    critical path

而不是简单降低频率。

============================================================
二十一、当前阶段不做这些事情
============================================================

本阶段禁止无计划扩展：

1. 不做 HDMI；
2. 不做摄像头；
3. 不让 PicoRV32 解 JPEG；
4. 不让 PicoRV32 解 PNG；
5. 不做 Linux；
6. 不使用 ARM A53；
7. 不做 PS DDR；
8. 不做 AXI DMA；
9. 不改 GCC 后端；
10. 不实现完整 RISC-V Vector Extension；
11. 不重新设计复杂流水线 CPU；
12. 不把项目变成 DMA 图像 IP。

项目核心必须始终是：

    RISC-V CPU
    +
    Custom ISA
    +
    PCPI
    +
    FPGA验证

============================================================
二十二、未来上板接口预留
============================================================

虽然当前没有实际板卡，
但 RTL 顶层要为 ZCU102 保留：

    clk
    reset
    uart_rx
    uart_tx
    led[]

最终真实上板预计使用：

    ZCU102 125MHz differential clock
    板载 PL UART
    reset button
    user LEDs

但是本阶段不要随便填写未经核实的 pin。

把 pin binding 留到 board bring-up 阶段。

============================================================
二十三、最终交付目录
============================================================

最终请保证至少拥有：

rtl/
    picorv32.v
    xpix_pcpi.v
    xpix_soc.v
    soc_bram.v
    simpleuart.v
    zcu102_top.v

firmware/
    xpix.h
    image_bench.c
    uart.c
    uart.h
    start.S
    sections.lds

host/
    image_to_raw.py
    raw_to_image.py
    make_test_image.py
    uart_host.py

sim/
    tb_xpix_pcpi.v
    tb_soc.v
    run_xpix.do
    run_soc.do
    run_all.*

vivado/
    create_project.tcl
    run_synth.tcl

docs/
    baseline_audit.md
    xpix_isa.md
    architecture.md
    memory_map.md
    verification_plan.md
    board_bringup_plan.md

reports/
    performance.csv
    xpix_firmware.dump
    regression.md
    ppa_summary.md
    vivado/...

============================================================
二十四、最终验收条件
============================================================

完成任务前，逐项确认：

[ ] PicoRV32 baseline 仿真通过

[ ] XPix 5条指令编码完成

[ ] XPix独立 RTL testbench 通过

[ ] >=100000 random vectors，0 mismatch

[ ] CPU实际执行 opcode=0x2B 成功

[ ] objdump确认 custom instruction 存在

[ ] Scalar图像算法正确

[ ] Packed RV32I算法正确

[ ] XPix算法正确

[ ] 三种算法结果 bit-exact

[ ] Motion Detection 256×256测试通过

[ ] mismatch_count = 0

[ ] cycle counter数据产生

[ ] instret counter数据产生

[ ] performance.csv真实生成

[ ] XPix相对Packed-RV32I确实得到正 speedup

如果出现 XPix 反而慢于 Packed-RV32I：

不要掩盖。

必须分析：

    PCPI latency
    loop overhead
    load/store比例
    compiler output

并尝试合理优化 XPix handshake，
例如从3-cycle优化到2-cycle或1-cycle，
前提是不破坏 timing。

[ ] 原 RV32I regression通过

[ ] 不使用 XPix 的普通程序无额外 cycle regression

[ ] Vivado baseline synthesis通过

[ ] Vivado XPix synthesis通过

[ ] utilization报告生成

[ ] timing报告生成

[ ] 125MHz目标进行验证

[ ] 所有脚本可以从干净工作区重新运行

============================================================
二十五、工作方式
============================================================

不要一次性写大量未经验证的 RTL。

严格按以下阶段执行：

Phase 0:
    repository audit

Phase 1:
    baseline PicoRV32 compile/sim

Phase 2:
    xpix_pcpi.v

Phase 3:
    XPix unit test

Phase 4:
    CPU + custom instruction minimal test

Phase 5:
    firmware + .insn

Phase 6:
    memory / BRAM / SoC

Phase 7:
    scalar / packed / XPix benchmark

Phase 8:
    image test

Phase 9:
    UART subsystem

Phase 10:
    RV32I regression

Phase 11:
    Vivado synthesis / timing

Phase 12:
    final report

每个 Phase：

1. 查看已有代码；
2. 实现；
3. 编译；
4. 运行；
5. 检查日志；
6. 如果失败就修复；
7. 测试通过以后再进入下一Phase。

不要仅仅告诉我：
“可以运行 xxx”。

你必须实际运行。

============================================================
二十六、任务结束时向我汇报
============================================================

最终不要只说“完成”。

请给我一份清晰摘要：

1. 新增/修改了哪些文件；
2. 是否修改 picorv32.v，若有为什么；
3. 5条自定义指令最终编码；
4. PCPI latency；
5. ModelSim所有测试结果；
6. random测试case数量；
7. mismatch数量；
8. 256×256图像测试结果；
9. Scalar/Packed/XPix cycles；
10. Scalar/Packed/XPix instret；
11. speedup结果；
12. Vivado LUT/FF/BRAM/DSP；
13. timing结果；
14. 当前是否达到125MHz；
15. 还有什么风险；
16. 真正拿到 ZCU102 后第一步应该干什么。

同时给出我下一阶段“ZCU102实物 bring-up”的精确步骤。

现在开始。

首先不要急着修改 RTL。

先：
    检查工作区
    审计 PicoRV32 仓库
    检查 vsim/vlog
    检查 Vivado
    检查 RISC-V GCC/binutils

然后向后推进并完成上述阶段。
```

---

这个 Prompt 我特意把一个很容易被忽略的问题加进去了：**必须比较 Scalar / Packed-RV32I / XPix 三组**。如果最终只是“普通 `uint8_t` C vs 4像素 SIMD”，老师完全可以质疑你的 speedup 大量来自 `LW` 一次加载 4 个像素；而加上 `Packed-RV32I` 以后，**XPix vs Packed-RV32I 才能真正证明自定义指令本身值不值。**

另外，在这个阶段我建议让 Work **先不要做真实 ZCU102 引脚约束和 bitstream**，而做到 **RTL 全通过 + firmware 全通过 + 256×256 图像仿真 bit-exact + Vivado synth/implementation 125 MHz timing clean**。到这里再接真实板，问题会从几十种压缩到基本只剩“时钟、复位、UART、XDC”四类，非常容易排查。
