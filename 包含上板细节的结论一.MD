我已经把你上传的 `picorv32-main.zip` 解开看过了，包括 `picorv32.v`、`testbench.v`、`picosoc/`、`firmware/`、`tests/` 和 Dhrystone。这个仓库非常适合做你这个 Project，而且我建议现在就把方案收敛下来，不要再换核。

我给你的最终方向是：

> **XPix-RV32：基于 PicoRV32 + PCPI 的 4×8-bit 图像 Packed-SIMD 自定义指令扩展，在 ZCU102 上完成真实灰度图像传输、处理、回传，并做 Baseline / 自定义指令的性能与 PPA 对比。**

最关键的是：**不改 PicoRV32 原来的 ALU 主数据通路，自定义指令外挂在 PCPI 上；一条指令同时处理 4 个 8-bit 像素。**这样既容易做，也非常容易回答老师最关心的两个问题：“为什么要加这几条指令？”以及“加完以后到底有没有变快？”

---

# 1. 先说清楚：你上传的这个 CPU 到底是什么

这里有一个特别容易搞混的地方：

**PicoRV32 源码“支持”RV32IMC，不代表你现在默认实例化出来的 CPU 就是 RV32IMC。**

我直接看了你这个 zip 里的 `picorv32.v`，默认参数是：

```verilog
ENABLE_COUNTERS       = 1
ENABLE_COUNTERS64     = 1
ENABLE_REGS_16_31     = 1
ENABLE_REGS_DUALPORT  = 1

TWO_STAGE_SHIFT       = 1
BARREL_SHIFTER        = 0

COMPRESSED_ISA        = 0

ENABLE_PCPI           = 0
ENABLE_MUL            = 0
ENABLE_FAST_MUL       = 0
ENABLE_DIV            = 0

ENABLE_IRQ            = 0
```

所以**裸 `picorv32` 默认配置基本就是 RV32I**，没有 M，没有 C，没有 IRQ，没有外部 PCPI。

整个源码可以配置成 RV32E/RV32I/RV32IC/RV32IM/RV32IMC，而且提供 native memory、AXI4-Lite、Wishbone 和 PCPI。([GitHub][1])

---

# 2. 当前 CPU 已经实现了哪些标准指令

我逐项检查了 `picorv32.v` 的 decoder。

### RV32I 基础整数指令

| 类别      | 已实现指令                                                       |
| ------- | ----------------------------------------------------------- |
| 上立即数    | `LUI`、`AUIPC`                                               |
| 跳转      | `JAL`、`JALR`                                                |
| 条件分支    | `BEQ`、`BNE`、`BLT`、`BGE`、`BLTU`、`BGEU`                       |
| Load    | `LB`、`LH`、`LW`、`LBU`、`LHU`                                  |
| Store   | `SB`、`SH`、`SW`                                              |
| 立即数 ALU | `ADDI`、`SLTI`、`SLTIU`、`XORI`、`ORI`、`ANDI`                   |
| 立即数移位   | `SLLI`、`SRLI`、`SRAI`                                        |
| 寄存器 ALU | `ADD`、`SUB`、`SLL`、`SLT`、`SLTU`、`XOR`、`SRL`、`SRA`、`OR`、`AND` |
| 其他      | `FENCE`、`ECALL/EBREAK`                                      |
| 性能计数    | `RDCYCLE[H]`、`RDTIME[H]`、`RDINSTRET[H]`                     |

但这里不要把它描述成完整的 privileged CPU。

它没有你通常在 Linux CPU 上看到的：

* MMU；
* cache；
* 浮点 F；
* Vector V；
* Atomic A；
* 完整机器态/特权态 CSR 系统。

所以非常适合定位成：

> **一个面积很小、结构简单、适合 FPGA 嵌入式控制和自定义指令研究的 32-bit RISC-V 软核。**

---

# 3. M、C 其实也已经有，只是默认没打开

源码里已经给了 PCPI 乘除法模块：

```text
picorv32_pcpi_mul
picorv32_pcpi_fast_mul
picorv32_pcpi_div
```

打开以后可以支持：

```text
MUL
MULH
MULHSU
MULHU

DIV
DIVU
REM
REMU
```

同时：

```verilog
COMPRESSED_ISA = 1
```

可以启用 RVC 压缩指令。

仓库自带的 `testbench.v` 甚至配置了：

```text
MUL = ON
DIV = ON
IRQ = ON
TRACE = ON
```

Makefile 默认还开了 C，因此**官方 testbench 跑的配置跟最原始 `picorv32` 默认配置不一样。**

PicoRV32 的目标本身就是面积/Fmax，而不是高 IPC。README 给出的典型平均 CPI 大约为 4；普通 ALU 大约 3 cycles，load 约 5，store 约 5，而不开 barrel shifter 时 shift 可以达到 4–14 cycles。([GitHub][2])

这个特点反而特别适合你的课程设计：

> **基础处理器比较简单，因此自定义领域指令获得明显 speedup 的空间很大。**

---

# 4. PicoRV32 不是五级流水 CPU

这个答辩一定要说对。

我看源码里面状态机有类似：

```text
fetch
ld_rs1
ld_rs2
exec
shift
ldmem
stmem
...
```

所以它是一个**多周期、FSM 驱动的微架构**，而不是：

```text
IF → ID → EX → MEM → WB
```

这种传统五级流水。

这一点其实对你反而有利。

你不需要解决：

* forwarding；
* RAW hazard；
* pipeline flush；
* custom instruction latency 插入流水线；
* custom EX stage timing。

自定义指令交给 PCPI，CPU等结果回来即可。

---

# 5. PCPI 正是这个项目最大的优势

源码已经把接口留好了：

```verilog
pcpi_valid
pcpi_insn
pcpi_rs1
pcpi_rs2

pcpi_wr
pcpi_rd
pcpi_wait
pcpi_ready
```

运行逻辑大概就是：

```text
CPU碰到自己不认识的指令
        ↓
pcpi_valid = 1
        ↓
把完整instruction + rs1 + rs2送出来
        ↓
XPix判断是不是自己的指令
        ↓
计算
        ↓
pcpi_rd = result
pcpi_wr = 1
pcpi_ready = 1
        ↓
CPU写入rd，继续执行
```

而且 PicoRV32 官方说明，如果 PCPI 操作比较长，协处理器可以拉高 `pcpi_wait`；如果长期没人响应则按非法指令处理。([GitHub][2])

所以我的建议是：

> **`picorv32.v` 尽可能一行不改。**

新增：

```text
xpix_pcpi.v
```

作为你自己的成果。

这比直接把 4 个运算 `if` 塞进 PicoRV32 ALU 更规范。

---

# 6. 最终基础 CPU 我建议这样配置

课程最终比较时，我不建议直接用 PicoSoC 默认 RV32IMC。

建议固定成：

```verilog
.ENABLE_COUNTERS(1),
.ENABLE_COUNTERS64(1),
.ENABLE_REGS_16_31(1),
.ENABLE_REGS_DUALPORT(1),

.BARREL_SHIFTER(1),

.COMPRESSED_ISA(0),

.ENABLE_MUL(0),
.ENABLE_FAST_MUL(0),
.ENABLE_DIV(0),

.ENABLE_IRQ(0),

.CATCH_MISALIGN(1),
.CATCH_ILLINSN(1)
```

Baseline：

```verilog
.ENABLE_PCPI(0)
```

XPix：

```verilog
.ENABLE_PCPI(1)
```

### 为什么我反而建议 BARREL_SHIFTER=1？

因为以后你的软件 baseline 很可能要用：

```text
shift
mask
shift
mask
```

处理 4 个 byte。

如果故意保持 PicoRV32 很慢的 iterative shift，会有人质疑：

> 你的 custom speedup 是不是故意把 baseline 弄得特别弱？

开 barrel shifter 会让比较**更加公平，而且更加有说服力**。

---

# 7. 自定义指令不要做“一个像素一条指令”

这是项目成败的核心。

老师右侧给了：

* 像素阈值；
* 饱和加法；
* 最大/最小；
* 绝对差。

如果你设计：

```text
ABS rd, rs1, rs2
MAX rd, rs1, rs2
```

但是每次只处理一个 8-bit pixel，意义一般。

我们应该利用 32-bit 寄存器：

```text
31          24 23          16 15           8 7            0
+-------------+-------------+-------------+-------------+
|   pixel 3   |   pixel 2   |   pixel 1   |   pixel 0   |
+-------------+-------------+-------------+-------------+
```

于是：

> **一条自定义指令同时处理 4 个 uint8 像素。**

这就是一个非常小的 Packed-SIMD。

---

# 8. 我现在建议做 5 条，不要只做 4 条

虽然要求是“至少 4 条”，但右边刚好有五个非常自然的操作。

我建议最后 ISA 就固定成：

| 指令            | 四个 lane 的操作    | 应用      |
| ------------- | -------------- | ------- |
| `PX.ABSDIFF8` | `abs(a-b)`     | 帧差/运动检测 |
| `PX.THRESH8`  | `a>=b ? 255:0` | 图像二值化   |
| `PX.ADDUS8`   | `min(a+b,255)` | 饱和亮度增强  |
| `PX.MAXU8`    | `max(a,b)`     | 最大值融合   |
| `PX.MINU8`    | `min(a,b)`     | 最小值融合   |

如果最后时间很多，第六条 Bonus 再考虑：

```text
PX.SAD8
```

即：

$$
|a_0-b_0|+|a_1-b_1|+|a_2-b_2|+|a_3-b_3|
$$

这个可以联系：

* block matching；
* 运动估计；
* 图像匹配。

但**第一阶段绝对不要做它**。

把前五个做扎实就已经足够出彩。

---

# 9. 五条指令究竟怎么工作

例如：

```text
rs1 = {10, 240, 80, 200}
rs2 = {30, 100, 50, 100}
```

### `PX.ABSDIFF8`

分别算：

```text
|10-30|    = 20
|240-100|  = 140
|80-50|    = 30
|200-100|  = 100
```

得到：

```text
rd = {20,140,30,100}
```

### `PX.MAXU8`

结果：

```text
{30,240,80,200}
```

### `PX.MINU8`

结果：

```text
{10,100,50,100}
```

---

# 10. 饱和加法为什么是个很好的展示指令

普通 uint8：

```text
250 + 20
```

如果普通 8-bit overflow：

```text
14
```

图像会突然从很亮变成很暗。

而饱和加法是：

$$
\min(250+20,255)=255
$$

你的硬件四路实现：

```verilog
wire [8:0] sum0 = a0 + b0;

out0 = sum0[8] ? 8'hff : sum0[7:0];
```

四份。

特别直观，而且硬件成本非常低。

---

# 11. `THRESH8` 怎么传 threshold

假设阈值：

```text
T = 80
```

软件构造：

```c
uint32_t threshold =
    0x50505050;
```

或者：

```c
uint32_t t4 = T * 0x01010101u;
```

于是：

```text
rs1 = p3 p2 p1 p0
rs2 = 80 80 80 80
```

指令一次得到：

```text
p3>=80 ? FF:00
p2>=80 ? FF:00
p1>=80 ? FF:00
p0>=80 ? FF:00
```

这特别适合做运动检测。

---

# 12. 指令编码建议现在就锁死

RISC-V 官方明确保留 `custom-0`、`custom-1` 等给自定义扩展，尤其 custom-0/custom-1 被推荐用于 32-bit custom instructions。([docs.riscv.org][3])

PicoRV32 自己的 IRQ custom ISA 会碰 `custom-0`。

所以我们直接避开它：

> **用 CUSTOM-1，opcode = `0x2B`。**

统一用 R-type：

```text
31       25 24     20 19     15 14 12 11      7 6       0
+----------+---------+---------+-----+----------+---------+
|  funct7  |   rs2   |   rs1   | f3  |    rd    | 0101011 |
+----------+---------+---------+-----+----------+---------+
```

定义：

```text
funct3 = 000   PX.ABSDIFF8
funct3 = 001   PX.THRESH8
funct3 = 010   PX.ADDUS8
funct3 = 011   PX.MAXU8
funct3 = 100   PX.MINU8
funct3 = 101   PX.SAD8      // optional
```

`funct7` 第一版全部设 0。

---

# 13. 硬件结构建议就做成这样

```text
                         +----------------------+
                         |      PicoRV32        |
                         |                      |
     BRAM <------------->| Native Memory I/F    |
                         |                      |
                         | RV32I ALU             |
                         |                      |
                         +----------+-----------+
                                    |
                                    | PCPI
                                    |
                  +-----------------v----------------+
                  |              XPix                |
                  |                                  |
                  | Decode custom-1                  |
                  |                                  |
                  | lane0 lane1 lane2 lane3          |
                  | 8bit  8bit  8bit  8bit           |
                  |                                  |
                  | ABS / THR / SATADD / MAX / MIN   |
                  +----------------------------------+
```

重点：

> **普通 RV32I 指令完全不经过 XPix。**

这是你防止“加指令导致普通 CPU 时序退化”的第一层保障。

---

# 14. XPix 第一版不要贪 1-cycle

我更建议做一个非常稳的 registered PCPI 单元。

### Cycle 0

CPU：

```text
pcpi_valid = 1
```

XPix：

```text
发现 opcode=0x2B
锁存 rs1
锁存 rs2
锁存 funct3
pcpi_wait = 1
```

### Cycle 1

四路运算。

寄存：

```text
result_reg
```

### Cycle 2

```text
pcpi_rd    = result_reg
pcpi_wr    = 1
pcpi_ready = 1
pcpi_wait  = 0
```

结束。

即使一条 XPix 指令占 2~3 个 CPU cycle，也完全不意味着慢。

因为它一次做：

> **4 个像素 × 一个原本需要多条 RV32I 指令的运算。**

后期 timing 很宽松时，再试一个：

```text
XPix-fast
```

做成更低 latency。

---

# 15. 最终性能实验不要只做“普通 C VS XPix”

这个非常重要。

如果只比较：

```text
for:
    uint8 load
    uint8 load
    scalar abs
    uint8 store
```

和：

```text
lw
lw
PX.ABSDIFF8
sw
```

别人会说：

> 你性能提升一大半是不是来自一次搬 4 个字节，而不是你的自定义指令？

所以我建议你有**三个软件版本**：

| 版本           | 特点                                               |
| ------------ | ------------------------------------------------ |
| Scalar-C     | 正常 `uint8_t` 图像代码                                |
| Packed-RV32I | 同样每次 `LW/LW/SW` 处理 4 像素，但靠 shift/mask/普通 RV32I 算 |
| Packed-XPix  | 同样 `LW/LW/SW`，中间用 XPix                           |

这个设计非常漂亮。

最后你能给出：

```text
Scalar-C        1,800,000 cycles
Packed-RV32I    1,100,000 cycles
XPix              320,000 cycles
```

那么：

```text
XPix vs scalar       = 5.63×
XPix vs packed-RV32I = 3.44×
```

第二个数字就是非常干净的：

> **ISA 加速收益。**

---

# 16. 上板到底处理什么图像

我建议不要做 Lenna 之类千篇一律的图。

最终准备三组图。

## 第一组：人工构造运动检测图

例如 256×256：

Frame A：

```text
灰色背景
白色方块位于 x=70~110
```

Frame B：

```text
同样背景
白色方块移动到 x=90~130
```

经过：

```text
A ─┐
   ├→ PX.ABSDIFF8 → PX.THRESH8 → motion mask
B ─┘
```

最终输出会出现移动前后物体的明显白色区域。

它的优点是：

> **你完全知道正确答案应该是什么。**

非常适合调试。

---

# 17. 第二组专门展示饱和加法

做一张灰度 gradient：

```text
0 → 255
```

执行：

```text
PX.ADDUS8(pixel, +80)
```

普通 overflow 错误版本：

```text
220+80 -> 44
```

你的版本：

```text
220+80 -> 255
```

结果特别容易肉眼说明：

> “这是图像处理为什么需要 saturating arithmetic。”

---

# 18. 第三组才是真实照片

答辩最终效果建议：

用你手机固定位置拍桌面两张照片。

第一张：

```text
桌面 + 笔
```

第二张：

```text
笔移动了
```

电脑端转：

```text
RGB/JPEG
   ↓
grayscale
   ↓
256 × 256
   ↓
uint8 raw
```

送 FPGA。

板子执行：

```text
ABSDIFF8
↓
THRESH8
↓
运动区域
```

然后把结果传回电脑。

你的 PPT 可以展示：

```text
Frame A       Frame B       CPU output
   🖼             🖼             🖼
                                   ↑
                              白色运动区域
```

这个现场效果比 LED 闪几下强太多。

---

# 19. 一个重要决定：不要让 PicoRV32 解 PNG/JPEG

**千万不要。**

PNG/JPEG 解码跟你的课题完全没有关系，而且会把项目复杂度炸掉。

正确路线：

```text
               PC
                |
        Python + Pillow
                |
      JPEG/PNG → Grayscale
                |
             Resize
                |
             uint8 RAW
                |
              UART
                ↓
            ZCU102
```

CPU收到的就是：

```text
65,536 bytes
```

对应：

```text
256 × 256 × 8 bit
```

它根本不知道什么是 PNG。

处理完成：

```text
ZCU102
 ↓ UART
PC Python
 ↓
RAW → Pillow Image
 ↓
output.png
```

---

# 20. 图像为什么定 256×256

这个尺寸非常合适：

$$
256\times256=65,536\ bytes=64KiB
$$

两张输入：

```text
128 KiB
```

两个结果：

```text
128 KiB
```

输入 A：

```text
64 KiB
```

输入 B：

```text
64 KiB
```

Scalar reference：

```text
64 KiB
```

XPix output：

```text
64 KiB
```

总共：

```text
256 KiB
```

所以直接给整个 SoC：

> **512 KiB BRAM**

非常舒服。

ZCU102 的 XCZU9EG 本身有 32.1 Mb Block RAM，大约 4 MB，所以给这个项目拿 512 KiB 完全合理。([AMD 文档][4])

甚至不需要碰 DDR。

---

# 21. 我建议第一版完全别碰 PS

ZCU102 是 Zynq UltraScale+，但不要因为有 A53 就非得用 A53。

第一版做：

```text
                    ZCU102 PL
+---------------------------------------------------+
|                                                   |
|       +-------------+                             |
|       | PicoRV32    |                             |
|       +------+------+                             |
|              |                                    |
|       Native Memory                              |
|              |                                    |
|      +-------+--------+                           |
|      | 512 KiB BRAM   |                           |
|      +----------------+                           |
|                                                   |
| PicoRV32 ---- PCPI ---- XPix                      |
|                                                   |
| PicoRV32 ---- MMIO ---- UART ---- USB ---- PC     |
|                                                   |
| PicoRV32 ---- MMIO ---- LEDs                      |
+---------------------------------------------------+
```

不需要：

```text
DDR
Linux
PS
AXI HP
DMA
HDMI
摄像头
```

这样项目风险会小非常多。

而且项目重点更加突出：

> **CPU + ISA + accelerator。**

---

# 22. ZCU102 甚至已经有现成 USB-UART 能利用

这个很舒服。

官方 ZCU102 文档显示，板上 CP2108 USB-to-Quad-UART 的 Channel 2 接到 PL：

```text
E13  UART2_TXD_O_FPGA_RXD
F13  UART2_RXD_I_FPGA_TXD
```

也就是说：

```text
PC → CP2108 TX → E13 → FPGA uart_rx
FPGA uart_tx → F13 → CP2108 RX → PC
```

([AMD 文档][5])

所以根本不用额外 USB-UART 小板。

电脑插 ZCU102 的 USB-UART 接口，就会看到多个 COM 口。

---

# 23. 仓库里面甚至已经有 UART RTL

你上传的：

```text
picosoc/simpleuart.v
```

我也看了。

它已经有：

```text
ser_tx
ser_rx
```

而且 PicoSoC 已经把它做成 memory-mapped peripheral：

```text
0x0200_0004   UART divider
0x0200_0008   UART data
```

所以**直接复用 `simpleuart.v` 就行。**

没必要自己重新造 UART。

我们只是不采用 PicoSoC 那套 SPI Flash boot。

---

# 24. ZCU102 时钟也非常方便

官方板卡文档有固定：

```text
CLK_125_P → G21
CLK_125_N → F21
```

频率：

```text
125 MHz
```

IO standard：

```text
LVDS_25
```

([AMD 文档][4])

所以可以：

```text
CLK_125_P/N
     ↓
   IBUFDS
     ↓
    BUFG
     ↓
125 MHz system clock
```

然后：

```text
PicoRV32
XPix
BRAM
UART
```

全部只跑一个时钟域。

**完全没有 CDC。**

这个对课程项目非常友好。

---

# 25. Reset 和 LED 也可以直接接

ZCU102 CPU RESET：

```text
AM13
active high
```

GPIO LED：

```text
LED0 AG14
LED1 AF13
LED2 AE13
LED3 AJ14
LED4 AJ15
LED5 AH13
LED6 AH14
LED7 AL12
```

都是 active-high。([AMD 文档][5])

我甚至建议 8 个灯这么定义：

| LED  | 含义                     |
| ---- | ---------------------- |
| LED0 | CPU alive / heartbeat  |
| LED1 | 正在收图                   |
| LED2 | 正在计算                   |
| LED3 | 计算完成                   |
| LED4 | Scalar 与 XPix 输出不一致    |
| LED5 | CPU trap               |
| LED6 | 执行过 custom instruction |
| LED7 | 通信/协议错误                |

答辩时这个特别实用。

哪怕 PC 软件突然有问题，也能看出 CPU 到底跑到哪一步。

---

# 26. 所以完整上板数据链应该这样

```text
手机照片
   ↓
PC Python
   ↓
Pillow:
resize(256×256)
convert("L")
   ↓
raw uint8[65536]
   ↓
pyserial
   ↓
USB
   ↓
ZCU102 CP2108
   ↓
simpleuart.v
   ↓
PicoRV32
   ↓
BRAM Frame_A / Frame_B
   ↓
运行算法
   ↓
BRAM Output
   ↓
UART
   ↓
PC Python
   ↓
output.png
```

这是我认为最合理的整体方案。

---

# 27. UART 第一阶段用 115200，最后可以上 921600

256×256 一张图：

```text
65536 byte
```

两张：

```text
131072 byte
```

115200 baud 下，两张传输大约要十几秒。

调试没问题。

最后可以试：

```text
921600 baud
```

两张图理论上只需要约 1.4 秒级。

但有一个原则：

> **UART 上传/下载时间绝对不要算进 XPix speedup。**

因为我们测的是 CPU 指令加速，不是 USB 性能。

最终报告分开：

```text
Host transfer time
CPU processing time
```

CPU performance 用 `RDCYCLE` 测。

---

# 28. PC→FPGA 协议不要做复杂

例如：

```text
byte 0..3   "XPIX"

byte 4      command

byte 5..6   width
byte 7..8   height

byte 9      parameter

后面：
frame A

如果需要：
frame B
```

命令定义：

```text
0x01 = ABSDIFF + THRESH
0x02 = SATURATION BRIGHTNESS
0x03 = MAX FUSION
0x04 = MIN FUSION
```

例如：

```text
command = 1
width   = 256
height  = 256
param   = 35
```

意思：

> 对 A/B 做帧差，然后用 35 做 threshold。

---

# 29. FPGA处理完之后回什么

我建议不要只回图。

返回：

```text
"RES0"

status

cycles_scalar
cycles_packed_rv32i
cycles_xpix

instret_scalar
instret_xpix

mismatch_count

65536 byte output image
```

PC 直接显示：

```text
---------------------------------
Image: 256 x 256
Scalar:        1,843,621 cycles
Packed RV32I:    972,384 cycles
XPix:            282,191 cycles

XPix / Scalar       = 6.53x
XPix / Packed RV32I = 3.45x

Mismatch = 0
---------------------------------
```

然后弹出：

```text
Input A
Input B
Output
```

这就是非常完整的现场 demo。

---

# 30. 更重要：板子自己做正确性对拍

512 KiB BRAM 就是为了让我们可以这么干。

保存：

```text
frame_A
frame_B

out_scalar
out_xpix
```

先执行：

```c
run_scalar();
```

然后：

```c
run_xpix();
```

最后：

```c
for (...) {
    if (out_scalar[i] != out_xpix[i])
        mismatch++;
}
```

必须：

```text
mismatch_count = 0
```

LED4 如果 mismatch 非零直接亮红灯/状态灯。

所以你答辩可以说：

> **不是“看输出图差不多”，而是 FPGA 上逐像素 bit-exact 自检。**

这会加很多分。

---

# 31. Cycle counter 就用 CPU 已经支持的

PicoRV32 已经有：

```text
RDCYCLE
RDINSTRET
```

所以测试函数：

```c
uint32_t t0 = rdcycle();

algorithm();

uint32_t t1 = rdcycle();

cycles = t1 - t0;
```

同时：

```text
rdinstret
```

还能测：

> 自定义指令到底减少了多少 retired instructions。

于是你最后不只是有 speedup：

```text
cycles ↓
```

还有：

```text
instruction count ↓
```

理论解释完整了。

---

# 32. 真正公平的最终实验矩阵

我甚至建议综合三套 bitstream。

### A：纯 RV32I

```text
PicoRV32
PCPI = OFF
XPix = none
```

### B：RV32I + PCPI shell

```text
PCPI = ON
XPix不真正使用/极小dummy
```

### C：RV32I + XPix

```text
PCPI = ON
完整5条指令
```

为什么这样做？

因为这样你能分出来：

$$
A\rightarrow B
$$

是：

> PCPI 接口本身带来的硬件成本。

而：

$$
B\rightarrow C
$$

是：

> 你真正 XPix arithmetic 的成本。

最终：

$$
A\rightarrow C
$$

是：

> 整个自定义 ISA 的总体代价。

这个实验设计已经明显比一般课程项目严谨。

---

# 33. “不能加了指令性能反而变差”怎么严谨证明

这个问题不能只靠嘴说。

最终做这张表：

| 项目               | RV32I | RV32I+PCPI | RV32I+XPix |
| ---------------- | ----: | ---------: | ---------: |
| LUT              |       |            |            |
| FF               |       |            |            |
| BRAM             |       |            |            |
| DSP              |       |            |            |
| WNS @125MHz      |       |            |            |
| 最大 Fmax          |       |            |            |
| Dhrystone cycles |       |            |            |
| 普通循环 cycles      |       |            |            |

其中最关键的是：

### 对“不使用 XPix 的程序”

应该有：

```text
CPU cycle count 基本完全一样
```

然后：

```text
125 MHz 全部 timing clean
```

如果 XPix 让 Fmax 从：

```text
190MHz → 185MHz
```

这种小下降不要怕。

你最终固定：

```text
125MHz
```

两者都能过 timing。

那么真实板载运行频率没有下降。

---

# 34. 然后对 XPix workload 测这个

| Kernel          | Scalar-C | Packed RV32I | XPix | XPix speedup |
| --------------- | -------: | -----------: | ---: | -----------: |
| ABS difference  |          |              |      |              |
| Threshold       |          |              |      |              |
| Saturating add  |          |              |      |              |
| MAX             |          |              |      |              |
| MIN             |          |              |      |              |
| Motion pipeline |          |              |      |              |

我暂时不会在没测之前承诺“10×”。

但设计目标可以定：

> **核心 kernel 至少 3×，争取 3～6×。**

这是比较可信的。

---

# 35. PPA 要怎么统计

老师幻灯片明确写了：

> 性能、功耗、硬件开销。

所以不能只报告 speedup。

最终至少：

```text
Vivado report_utilization
Vivado report_timing_summary
Vivado report_power
```

整理：

| 指标            | Baseline | XPix | 增幅 |
| ------------- | -------: | ---: | -: |
| LUT           |          |      |    |
| FF            |          |      |    |
| BRAM          |          |      |    |
| DSP           |          |      |    |
| Fmax          |          |      |    |
| Dynamic Power |          |      |    |
| cycles/frame  |          |      |    |
| Energy/frame  |          |      |    |

而且这里还有一个很值得讲的指标：

$$
Energy/frame=P\times T
$$

哪怕：

```text
XPix瞬时动态功耗 +5%
```

但如果：

```text
execution time -70%
```

那么：

```text
energy / frame
```

依然可能明显下降。

这就是体系结构项目比较好的分析方式。

---

# 36. 图像测试我建议最终做四项

你老师给的几个方向刚好都可以覆盖：

| Demo   | 使用自定义指令              | 视觉效果  |
| ------ | -------------------- | ----- |
| 运动检测   | `ABSDIFF8 + THRESH8` | ★★★★★ |
| 亮度增强   | `ADDUS8`             | ★★★★★ |
| 两图最亮融合 | `MAXU8`              | ★★★★  |
| 两图最暗融合 | `MINU8`              | ★★★★  |

最终汇报的主角只需要：

> **运动检测。**

其他三个用一两页表明五条指令全部有实际应用。

---

# 37. 为什么不建议第一版做形态学腐蚀/膨胀

MAX/MIN 确实可以进一步做：

```text
grayscale dilation
grayscale erosion
```

但这样需要处理：

```text
左邻居
右邻居
上一行
下一行
```

数据访问开始复杂。

第一版做：

```text
MAX(A,B)
MIN(A,B)
```

这种两图融合，结果最干净。

等基本分全部拿到以后，如果还想冲亮点，可以把 max/min 用到 3×3 morphology。

---

# 38. 代码目录我建议现在就重新组织

不要直接在官方仓库里到处乱改。

保留 upstream：

```text
rtl/
├── picorv32.v             # 原版，尽量完全不改
├── simpleuart.v           # 从picosoc复用
├── xpix_pcpi.v            # 核心成果
├── xpix_soc.v             # RAM/MMIO/CPU连接
├── soc_bram.v
└── zcu102_top.v

firmware/
├── start.S
├── sections.lds
├── xpix.h
├── image_bench.c
└── uart.c

host/
├── send_image.py
├── make_test_image.py
└── images/

sim/
├── tb_xpix_pcpi.v
├── tb_soc.v
└── reference/

constraints/
└── zcu102.xdc

reports/
├── baseline/
├── pcpi/
└── xpix/
```

---

# 39. C里面怎么调用 XPix

不用魔改 GCC。

第一阶段直接：

```c
static inline uint32_t px_absdiff8(
    uint32_t a,
    uint32_t b)
{
    uint32_t r;

    asm volatile(
        ".insn r 0x2b, 0, 0, %0, %1, %2"
        : "=r"(r)
        : "r"(a), "r"(b)
    );

    return r;
}
```

类似：

```text
funct3=0 → absdiff
funct3=1 → threshold
funct3=2 → addus
funct3=3 → max
funct3=4 → min
```

这样无需：

```text
改 GCC
改 binutils
改 LLVM
```

极大降低工程风险。

---

# 40. XPix 的 RTL 本身其实不会很大

四个 lane：

```text
lane 0 → 8 bit
lane 1 → 8 bit
lane 2 → 8 bit
lane 3 → 8 bit
```

每 lane 本质是：

```text
8-bit comparator
8/9-bit adder
subtractor
mux
```

没有乘法。

所以预计：

```text
DSP = 0
```

只需要少量 LUT/FF。

这特别适合你报告：

> **用很小的硬件成本换取明显的 domain-specific speedup。**

---

# 41. 上板前验证不能省

开发顺序我建议严格按照：

### 阶段 1：原 CPU

先不加 XPix：

```text
PicoRV32
BRAM
UART
LED
```

上 ZCU102。

确保：

```text
Hello
UART RX/TX
LED
```

都正常。

---

### 阶段 2：图像通信

仍然不加 XPix。

PC：

```text
test.raw
↓
UART
```

板：

```text
BRAM
↓
UART echo
```

PC：

```text
原图 == 回传图
```

必须逐字节一致。

---

### 阶段 3：单独 XPix RTL

此时还不接 CPU。

给：

```text
100000 组 random rs1/rs2
```

拿 Python/C golden model 对拍。

边界必须覆盖：

```text
0 + 0
255 + 1
255 + 255

abs(0-255)
abs(255-0)
abs(128-128)

threshold:
79
80
81
```

全部 0 mismatch。

---

### 阶段 4：CPU + XPix 仿真

真正让 CPU 执行：

```text
.insn
```

验证：

```text
rd
rs1
rs2
```

还测：

```text
连续custom
rd=x0
不同funct3
```

---

### 阶段 5：原 RV32I regression

仓库已经有：

```text
tests/
firmware/
testbench.v
```

利用现有测试证明：

> 加 XPix 没有把原来的 RV32I 搞坏。

仓库本来就提供 instruction tests、firmware 和 Dhrystone。([GitHub][6])

---

### 阶段 6：ZCU102 跑真实图像

最后才进入：

```text
PC图片
→ UART
→ BRAM
→ scalar
→ XPix
→ compare
→ 返回结果
```

---

# 42. ZCU102 的首个 XDC 大概长这样

时钟：

```tcl
set_property PACKAGE_PIN G21 [get_ports clk125_p]
set_property PACKAGE_PIN F21 [get_ports clk125_n]
set_property IOSTANDARD LVDS_25 [get_ports {clk125_p clk125_n}]

create_clock -period 8.000 [get_ports clk125_p]
```

PL UART：

```tcl
set_property PACKAGE_PIN E13 [get_ports uart_rx]
set_property IOSTANDARD LVCMOS33 [get_ports uart_rx]

set_property PACKAGE_PIN F13 [get_ports uart_tx]
set_property IOSTANDARD LVCMOS33 [get_ports uart_tx]
```

Reset：

```tcl
set_property PACKAGE_PIN AM13 [get_ports reset_btn]
set_property IOSTANDARD LVCMOS33 [get_ports reset_btn]
```

LED：

```tcl
set_property PACKAGE_PIN AG14 [get_ports led[0]]
set_property PACKAGE_PIN AF13 [get_ports led[1]]
...
```

这些引脚来自 ZCU102 UG1182，但正式出 bitstream 前还是要根据你们实验室**实际 ZCU102 board revision / master XDC 再核一遍**；AMD 文档本身也提醒 ZCU102 存在不同 revision。([AMD 文档][4])

---

# 43. 整个项目最漂亮的一张体系结构图最终应该是

```text
                       ┌─────────────────┐
                       │     Host PC     │
                       │                 │
PNG/JPEG ─────────────►│ Python/Pillow   │
                       │ grayscale/raw   │
                       └────────┬────────┘
                                │ USB UART
                                │
             ZCU102 PL          ▼
┌────────────────────────────────────────────────────┐
│                    SimpleUART                       │
│                        │                            │
│                        ▼                            │
│                ┌───────────────┐                    │
│                │   PicoRV32    │                    │
│                │    RV32I      │                    │
│                └───────┬───────┘                    │
│                        │                             │
│          ┌─────────────┼───────────────┐             │
│          │             │               │             │
│          ▼             ▼               ▼             │
│      512KB BRAM      MMIO           PCPI             │
│                                          │           │
│                                          ▼           │
│                                ┌─────────────────┐    │
│                                │ XPix 4×8 SIMD   │    │
│                                │ ABS / THR       │    │
│                                │ SAT / MAX / MIN │    │
│                                └─────────────────┘    │
└────────────────────────────────────────────────────┘
```

---

# 44. 从“拿高分”的角度，最应该强调的不是“五条指令”

你的项目真正能拉开差距的是下面这条完整逻辑：

**问题：**

```text
8-bit图像逐像素运算存在大量数据级并行
```

↓

**分析：**

```text
RV32I寄存器是32 bit，
但scalar代码通常一次只有效处理一个8-bit pixel
```

↓

**体系结构设计：**

```text
将32-bit拆成4×8-bit SIMD lane
```

↓

**ISA：**

```text
ABSDIFF8 / THRESH8 / ADDUS8 / MAXU8 / MINU8
```

↓

**微架构：**

```text
PCPI外挂，不侵入CPU主ALU
```

↓

**硬件证明：**

```text
LUT / FF / Fmax / Power
```

↓

**软件证明：**

```text
Scalar
Packed RV32I
Packed XPix
```

↓

**FPGA证明：**

```text
ZCU102真实图片输入
```

↓

**结果：**

```text
bit-exact
0 mismatch
N× speedup
低面积开销
普通程序无退化
```

这就是一套非常完整的**领域专用指令集扩展（DSA/ASIP）**故事。

---

# 45. 我现在最建议锁死的项目规格

| 项目            | 最终决定                            |
| ------------- | ------------------------------- |
| CPU           | PicoRV32                        |
| Base ISA      | RV32I                           |
| 微架构           | PicoRV32 原多周期 FSM               |
| CPU源码         | 尽量不改                            |
| 自定义接口         | PCPI                            |
| Custom opcode | custom-1 / `0x2B`               |
| 自定义指令         | 5 条                             |
| 并行度           | 4×8-bit                         |
| 图像格式          | Grayscale uint8                 |
| 图像尺寸          | 256×256                         |
| 主应用           | 帧差运动检测                          |
| 其他 Demo       | 饱和增强、MAX、MIN                    |
| FPGA          | ZCU102                          |
| CPU Clock     | 125 MHz 目标                      |
| Memory        | 512 KiB BRAM                    |
| 图像输入          | PC → USB UART → PL              |
| 图像输出          | PL → USB UART → PC              |
| 软件工具          | Python Pillow + pyserial        |
| 正确性           | Scalar/XPix 板上逐字节对比             |
| 性能            | `rdcycle` / `rdinstret`         |
| PPA           | Vivado utilization/timing/power |
| 关键 baseline   | Scalar-C + Packed-RV32I + XPix  |

---

## 最后一个非常重要的判断

**现在不要去加 AXI DDR、摄像头、HDMI，也不要改成五级流水，更不要改编译器。**

那些东西会把大量时间耗在“外围系统工程”，但对这个课程的核心评分点：

```text
CPU
自定义指令
加速效果
FPGA验证
PPA
```

帮助很有限。

现在最值钱的下一步其实已经非常明确：

> **第一步先在你这份源码基础上搭一个 `xpix_pcpi.v + xpix_soc.v + BRAM + simpleuart + zcu102_top.v` 的最小工程；先保证原版 PicoRV32 在 ZCU102 上能通过 UART 收发，再加入五条 XPix。**

如果严格按这个路线做，最终你不仅能回答“我实现了哪些指令”，还能回答更难的：**为什么这么设计、为什么确实是自定义指令带来的加速、增加多少硬件、有没有降低原 CPU 性能，以及真实 FPGA 上是不是 bit-exact 正确。**这几个才是最容易把项目从“完成作业”抬到比较完整的课程设计的部分。

[1]: https://github.com/yosyshq/picorv32?utm_source=chatgpt.com "GitHub - YosysHQ/picorv32: PicoRV32 - A Size-Optimized RISC-V CPU · GitHub"
[2]: https://github.com/YosysHQ/picorv32/blob/main/README.md?utm_source=chatgpt.com "picorv32/README.md at main · YosysHQ/picorv32 · GitHub"
[3]: https://docs.riscv.org/reference/isa/v20240411/_attachments/riscv-unprivileged.pdf?utm_source=chatgpt.com "The RISC-V Instruction Set Manual Volume I: Unprivileged Architecture"
[4]: https://docs.amd.com/api/khub/documents/rt6tROKcAceWoROXFPMY5w/content "ZCU102 Evaluation Board User Guide"
[5]: https://docs.amd.com/api/khub/documents/rt6tROKcAceWoROXFPMY5w/content?utm_source=chatgpt.com "ZCU102 Evaluation Board User Guide"
[6]: https://github.com/YosysHQ/picorv32?utm_source=chatgpt.com "GitHub - YosysHQ/picorv32: PicoRV32 - A Size-Optimized RISC-V CPU · GitHub"
